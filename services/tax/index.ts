/**
 * services/tax/
 *
 * Future home of all tax-domain service functions.
 * Migration happens here gradually — one file per operation.
 *
 * Planned files (Step 2+):
 *   fetchClients.ts
 *   fetchClient2307s.ts
 *   fetchPriorYearCredit.ts
 *   fetchTaxPayments.ts
 *   fetchSubmissions.ts
 *   recordSawtSubmission.ts
 *   buildBatchSAWTQueue.ts
 *
 * NOTE: services/tax.ts (legacy) stays active during migration.
 * Do not delete it until all callers are updated.
 */

export {};